<view 
  onTap="onWebView" 
  class="links {{under_line == true ? 'under-line': ''}}" 
  style="color: {{color}}">
  <view>
    <slot>Hiperlink Button</slot></view>
  <image
    a:if="{{stateImg}}"
    style="background-color: {{color}}"
    mode="scaleToFill"
    src="{{srcImg}}"
  />
</view>